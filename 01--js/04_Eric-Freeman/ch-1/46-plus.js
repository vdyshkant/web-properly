var age = 25;
var name = "Owen";

if (age < 14) {
  alert("Sorry this page is for kids only!");
} else {
  alert("Welcome " + name + "!");
}

    // eof
    // p47:

var winners = 2;
var name = "Duke";
var isEligible = false;

var losers;

    // eof
    // p50:

// test for jokes
var joke = "JavaScript walked into a bar.....";
var toldJoke = false;
var $punchline = "Better watch out for whose semi-colons.";
var entage = 20;
var result;

if (toldJoke == true) {
  alert($punchline);
} else {
  alert(joke);
}
// Movie Night
var zipCode = 98104;
var joesFavoriteMovie = "ForBidden Planet";
var movieTicket$ = 9;

if (movieTicket$ >= 9) {
  alert("Too much!");
} else {
  alert("We're going to see " + joesFavoriteMovie);
}

    // eof
    // p51

var price = 10;
var discount = 20;
var total = price - (price * (discount / 100));
console.log(total);


var text = "Dear" + "Reader" + "," // "Dear Reader,"


age < 14;
cost >= 3.99;
animal == "bear";
